import { Login.PasswordDirective } from './login.password.directive';

describe('Login.PasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new Login.PasswordDirective();
    expect(directive).toBeTruthy();
  });
});
